export const LONG_HORIZON_CASES = Object.freeze([
    {
        name: 'targeted-bug-fix',
        prompt: [
            'TASK::targeted-bug-fix',
            'Inspect this small JavaScript repository, find the incorrect clamp behavior in src/calculator.js,',
            'make the smallest exact edit, inspect the change, and run node --test before finishing.',
        ].join(' '),
        expected: { completion: 'target-tool', targetTool: 'finish_task' },
        task: 'targeted-bug-fix',
        targetFile: 'src/calculator.js',
        requiredReadFiles: ['src/calculator.js'],
        modifiedFiles: ['src/calculator.js'],
    },
    {
        name: 'cross-file-change',
        prompt: [
            'TASK::cross-file-change',
            'Inspect src/config.js and src/runner.js. runner.js must use the DEFAULT_RETRIES value',
            'defined in config.js. Search both files, make the smallest exact edit, inspect it,',
            'and run node --test before finishing.',
        ].join(' '),
        expected: { completion: 'target-tool', targetTool: 'finish_task' },
        task: 'cross-file-change',
        targetFile: 'src/runner.js',
        requiredReadFiles: ['src/config.js', 'src/runner.js'],
        modifiedFiles: ['src/runner.js'],
    },
    {
        name: 'failed-first-search',
        prompt: [
            'TASK::failed-first-search',
            'Use repository search to find the clamp implementation in src/calculator.js.',
            'An initial search may return no matches; adapt the query or use glob, inspect the source,',
            'fix the clamp behavior, inspect the edit, and run node --test before finishing.',
        ].join(' '),
        expected: { completion: 'target-tool', targetTool: 'finish_task' },
        task: 'failed-first-search',
        targetFile: 'src/calculator.js',
        requiredReadFiles: ['src/calculator.js'],
        modifiedFiles: ['src/calculator.js'],
    },
    {
        name: 'failed-edit-recovery',
        prompt: [
            'TASK::failed-edit-recovery',
            'Fix the clamp behavior in src/calculator.js using an exact edit. If the first edit fails,',
            're-read the source and recover. Inspect the final edit and run node --test before finishing.',
        ].join(' '),
        expected: { completion: 'target-tool', targetTool: 'finish_task' },
        task: 'failed-edit-recovery',
        targetFile: 'src/calculator.js',
        requiredReadFiles: ['src/calculator.js'],
        modifiedFiles: ['src/calculator.js'],
    },
    {
        name: 'large-context-fix',
        prompt: [
            'TASK::large-context-fix',
            'Inspect the large src/legacy-catalog.js compatibility notes for CLAMP_POLICY_MIN_FIRST,',
            'then fix the clamp behavior in src/calculator.js. Search, read, make the smallest exact edit,',
            'inspect it, and run node --test before finishing.',
        ].join(' '),
        expected: { completion: 'target-tool', targetTool: 'finish_task' },
        task: 'large-context-fix',
        targetFile: 'src/calculator.js',
        requiredReadFiles: ['src/legacy-catalog.js', 'src/calculator.js'],
        modifiedFiles: ['src/calculator.js'],
    },
])
