/**
 * Generic external Cordis / DSH plugins can still be listed here for hosts
 * that use external-plugins.js directly. MCP servers belong in mcp.config.js
 * and are managed by McpManager so they are not loaded twice.
 */
export default []
